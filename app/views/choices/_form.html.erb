<%= form_with(model: choice) do |form| %>
  <% if choice.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(choice.errors.count, "error") %> prohibited this choice from being saved:</h2>

      <ul>
        <% choice.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :poll_id, style: "display: block" %>
    <%= form.text_field :poll_id %>
  </div>

  <div>
    <%= form.label :option_text, style: "display: block" %>
    <%= form.text_field :option_text %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
